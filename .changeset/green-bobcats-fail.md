---
"@digitalmedika/satusehat": minor
---

Add `createEncounterConditionBuilder` to support simple post-encounter IGD flows from `Encounter` to `Condition`, including helper output for `Encounter.diagnosis`.
